List:
- Export as BioOpt

Design:
- Save
- Save as

Reactions:
All done

Metabolites:
- Consumed by Tooltips uses name, not id
- Clicking reactions uses name, not id

Settings:
- FBA settings Toggle
- FBA select vis reaction
- Axis labeling

Simulation:
- Export as image
- Export as vector
- RA sim (-> Chart)
- Combine with previous sim

Dialog Reaction:
- Edit reaction (FIXME: stoichiometry UI)
- Constraints min/max must be from model
- Invalidate rendering of metabolites that are not used anymore

Dialog Bulk add reaction
- Reactions not added

Graph:
- Reduce graph when the network is huge
